## 1. 本地服务桥接基础

- [x] 1.1 建立本地后端最小健康检查与前端 service bridge，统一处理真实 API、mock fallback、加载状态和错误模型
- [x] 1.2 调整前端 stores 和页面数据入口，移除页面对 `mockApi` 的直接依赖，并在应用壳层展示后端连接状态

## 2. 项目与配置管理

- [x] 2.1 实现项目输出路径和小说参数的后端读写接口，并在前端项目/设置页完成加载、编辑、保存和校验
- [x] 2.2 实现 LLM 多配置、Embedding、代理和生成阶段模型选择的后端读写/测试接口，并补齐前端配置表单状态

## 3. 文件编辑工作区

- [x] 3.1 实现核心文本文件接口，支持 `Novel_setting.txt`、`Novel_directory.txt`、`character_state.txt`、`global_summary.txt` 的加载、保存和字数统计
- [x] 3.2 实现章节文件接口和章节编辑体验，支持章节列表、上一章/下一章、保存、刷新、字数统计和未保存变更保护

## 4. 生成任务接入

- [x] 4.1 实现生成任务接口，支持设定、目录、草稿、定稿、一致性审校的任务创建、状态查询、日志和错误返回
- [x] 4.2 实现批量生成接口和前端参数对话流程，支持起止章节、目标字数、最低字数、自动扩写和任务进度展示

## 5. 知识库、角色库与 WebDAV 工具

- [x] 5.1 实现知识库导入、向量库清理和剧情要点查看接口，并在前端知识库/工作台入口补齐确认、结果和失败状态
- [x] 5.2 实现角色库基础接口和前端基础管理体验，覆盖分类/角色列表、查看、编辑、保存、导入入口和选择角色写入章节参数
- [x] 5.3 实现 WebDAV 测试连接、备份和恢复接口，并在前端设置页补齐脱敏输入、确认、结果反馈和恢复后刷新

## 6. 验证与兼容性

- [x] 6.1 补充 Python 单元/API 测试、前端类型检查和构建验证，确认 `python main.py` 仍可启动且旧 GUI 可读取前端保存的兼容文件
