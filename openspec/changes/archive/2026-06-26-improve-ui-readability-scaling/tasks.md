## 1. 样式基础

- [x] 1.1 新增共享 UI 样式模块，定义字体族、普通字体、编辑器字体、小号字体和舒适版控件缩放比例。
- [x] 1.2 在应用启动构建界面前应用 CustomTkinter 舒适版控件缩放。

## 2. 主界面字体替换

- [x] 2.1 将主功能页左侧标题、步骤按钮、日志区和章节编辑区替换为共享字体常量。
- [x] 2.2 将右侧配置页、小说参数区、可选功能按钮区的常见控件替换为共享字体常量。
- [x] 2.3 将架构、目录、角色状态、全局摘要和章节管理页的长文本区替换为编辑器字体常量。

## 3. 弹窗和辅助界面

- [x] 3.1 将生成确认弹窗、批量生成弹窗、导入选择弹窗等常用弹窗替换为共享字体常量。
- [x] 3.2 将角色库窗口和角色导入/管理相关控件替换为共享字体常量。
- [x] 3.3 保留帮助问号等小型辅助按钮的小号字体，避免按钮尺寸异常。

## 4. 布局适配与验证

- [x] 4.1 检查并适度调整右侧面板或关键控件宽度，避免舒适版字号导致明显挤压。
- [x] 4.2 使用 `rg "Microsoft YaHei"` 检查剩余硬编码字体，确认保留项有明确理由。
- [x] 4.3 运行 `python -m pytest tests` 验证现有自动化测试。
- [x] 4.4 启动 `python main.py` 检查主窗口可打开，主要页面控件无明显重叠或不可用裁剪。
