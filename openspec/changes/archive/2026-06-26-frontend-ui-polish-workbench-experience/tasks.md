## 1. 信息架构与组件基础

- [x] 1.1 梳理现有页面重复结构，确定首批共享组件命名和目录位置。
- [x] 1.2 抽出页面标题/说明/主操作区域组件，并替换项目、工作台、章节、生成、知识库、设置页面的重复 header。
- [x] 1.3 抽出表单分组、操作栏、状态提示和空状态组件，统一 loading、success、error、warning 呈现。
- [x] 1.4 抽出长文本编辑/查看组件，支持只读、编辑、字数统计、保存状态和占位状态。

## 2. 设置页重构

- [x] 2.1 将设置页拆为配置 tabs 或等价分组：项目参数、LLM、Embedding、阶段模型/代理、WebDAV。
- [x] 2.2 保留现有 `serviceBridge` 读写行为，确保保存全部、测试 LLM、WebDAV 测试/备份/恢复仍可用。
- [x] 2.3 用统一状态提示替换页面内分散的保存、测试和错误消息。
- [x] 2.4 检查设置页在常见桌面宽度下表单不拥挤，字段标签和按钮不重叠。

## 3. 知识库页重构

- [x] 3.1 将知识库页拆为 tabs：知识文件、剧情要点、角色库。
- [x] 3.2 知识文件 tab 保留导入资料、清理向量库、文件列表和结果状态。
- [x] 3.3 剧情要点 tab 使用长文本查看组件展示 `plot_arcs.txt` 内容、字数和缺失状态。
- [x] 3.4 角色库 tab 保留分类列表、角色选择、导入、编辑保存和写入章节参数操作。
- [x] 3.5 将清理向量库等破坏性操作改为统一确认区或确认组件。

## 4. 生成页重构

- [x] 4.1 将生成页拆成任务创建面板、批量参数面板、任务列表和任务详情/日志区域。
- [x] 4.2 批量参数保持 start/end chapter、target/minimum words 和 auto enrich 行为不变。
- [x] 4.3 任务列表用于扫描状态，任务详情用于展示日志、错误和进度，避免所有日志铺满列表。
- [x] 4.4 用统一状态提示展示任务创建失败、加载中和空任务状态。

## 5. 工作台流程收敛

- [x] 5.1 调整工作台页，使其聚焦当前项目、章节焦点、核心文件状态和下一步生成动作。
- [x] 5.2 确认主导航顺序和页面标题能体现项目 → 工作台 → 章节 → 生成 → 知识库 → 设置的主流程。
- [x] 5.3 确认右侧上下文区域与页面主内容不重复展示大段相同信息。

## 6. 验证与收尾

- [x] 6.1 运行 `cd frontend && npm run typecheck`。
- [x] 6.2 运行 `cd frontend && npm run build`。
- [x] 6.3 启动本地 API 和前端，手动检查设置、知识库、生成、章节、工作台页面基础交互。
- [x] 6.4 检查后端不可用或 mock fallback 时的状态展示清晰，不误导用户认为真实保存已完成。
- [x] 6.5 更新项目根目录 `AGENTS.md` 中新增的前端 UI 组件和页面组织约定。
